# 課題一覧

## 課題1：顧客情報取得APIの作成

### Controller

- CustomersController

### 要件

- DBへ接続する
- 顧客マスタ（Customers）からデータを取得する
- 取得したデータをJSON形式へ変換する
- APIレスポンスとして返却する

### 想定URL

```http
GET /api/customers
```

### レスポンス例

```json
[
  {
    "customerId": 1,
    "customerCode": "C001",
    "customerName": "株式会社ABC",
    "address1": "東京都千代田区1-1-1",
    "tel": "03-1111-1111"
  }
]
```

---

## 課題2：帳票情報取得APIの作成（JSON形式）

### Controller

- ReportsController

### 要件

- DBへ接続する
- 帳票出力用SQLを実行する
- 取得したデータをJSON形式へ変換する
- APIレスポンスとして返却する

### 想定URL

```http
GET /api/reports/ORD0001
```

### レスポンス例

```json
[
  {
    "orderNo": "ORD0001",
    "orderDate": "2026-06-01",
    "customerCode": "C001",
    "customerName": "株式会社ABC",
    "productCode": "P001",
    "productName": "ノートPC",
    "quantity": 2,
    "unitPrice": 120000,
    "amount": 240000
  }
]
```

---

## 課題3：帳票情報取得APIの作成（CSV形式）

### Controller

- ReportsController

### 要件

- DBへ接続する
- 帳票出力用SQLを実行する
- 取得したデータをCSV形式へ変換する
- CSVファイルとしてダウンロードできるようにする
- APIレスポンスとして返却する

### 想定URL

```http
GET /api/reports/csv/ORD0001
```

### レスポンスヘッダ

```http
Content-Type: text/csv
Content-Disposition: attachment; filename="report.csv"
```

### CSV出力例

```csv
OrderNo,OrderDate,CustomerCode,CustomerName,ProductCode,ProductName,Quantity,UnitPrice,Amount
ORD0001,2026-06-01,C001,株式会社ABC,P001,ノートPC,2,120000,240000
ORD0001,2026-06-01,C001,株式会社ABC,P002,モニター,3,35000,105000
ORD0001,2026-06-01,C001,株式会社ABC,P003,キーボード,5,5000,25000
```

---

# 学習ポイント

- ASP.NET Web API の基本構成
- Controller の作成方法
- SQL Server への接続方法
- ADO.NET を利用したDBアクセス
- JSONレスポンスの返却方法
- CSVファイル生成方法
- FileResult を利用したファイルダウンロード
- 例外処理（try-catch）
- 接続文字列（Web.config）の利用方法

---

# 発展課題

- OrderNoをURLパラメータで受け取る
- Repositoryクラスを作成してDB処理を分離する
- DTOクラスを作成する
- 非同期処理（async/await）に対応する
- PDF帳票出力に対応する